# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in supercast.gemspec
gemspec

group :test do
  # Use for linting our Ruby code
  gem 'rubocop'

  # Use for code coverage on tests
  gem 'simplecov'
end
